import 'package:flutter/material.dart';
import 'Componentes/body.dart';

class LoginScreen_Cliente extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Body(),
    );
  }

  Future login() async {
    var url = "";
  }
}
